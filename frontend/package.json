{
  "name": "solvit-frontend",
  "version": "2.0.0",
  "description": "Frontend for SolvIt AI Problem Solver",
  "main": "src/pages/index.html",
  "scripts": {
    "start": "serve src/pages -l ${PORT:-8080}",
    "dev": "serve src/pages -l ${PORT:-8080}",
    "build": "echo 'No build process needed for static files'",
    "serve": "serve src/pages -l ${PORT:-8080}"
  },
  "dependencies": {
    "serve": "^14.2.6"
  },
  "keywords": [
    "frontend",
    "web",
    "problem-solver"
  ],
  "author": "SolvIt Team",
  "license": "MIT"
}
